

import { Playground } from 'docz';
import { WindowCalendar } from '../src/core';


# Basic usage

## Without props

<Playground>
<WindowCalendar/>
</Playground>

## Min date / Max date

<Playground>
<WindowCalendar
  min={new Date(2019, 0)} // Minimum month to render
  minDate={new Date(2019, 0, 10)} // Minimum selectable date
  max={new Date(2019, 11)} // Maximum month to render
  maxDate={new Date(2019, 10, 15)} // Minimum selectable date
/>
</Playground>

## Disabled dates

<Playground>
<WindowCalendar
  disabledDates={[
    new Date(2019, 4, 10),
    new Date(2019, 4, 13),
    new Date(2019, 4, 17),
  ]}
/>
</Playground>

## Disabled weekdays

<Playground>
<WindowCalendar disabledDays={[0, 6]} />
</Playground>
